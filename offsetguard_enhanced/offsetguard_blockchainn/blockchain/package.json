{
  "name": "offsetguard-blockchain",
  "version": "1.0.0",
  "description": "OffsetGuard CarbonCreditNFT — ERC-721 smart contract on Ganache",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.4.5"
  }
}
